fos_elastica:
    serializer: ~
    clients:
        default:
            host: "%elasticsearch.host%"
            port: "%elasticsearch.port%"
            retryOnConflict: 2
            logger: false
